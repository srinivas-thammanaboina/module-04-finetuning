# --- Secrets (NEVER commit) --------------------------------------------------
.env
.env.*
*.key

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# --- macOS -------------------------------------------------------------------
.DS_Store

# --- API response cache (regenerable; can get large; may echo chunk text) ----
data/.cache/

# --- Model artifacts (large binaries; produced in Stage 4/5) -----------------
adapter/
model_merged/
*.gguf
*.bin
*.safetensors
*.pt
*.ckpt

# NOTE: data/train.jsonl, data/eval.jsonl, data/SPLIT.md and notes/ ARE tracked
# on purpose — the dataset and the before/after are the teaching artifacts of
# this module (CLAUDE.md Rule 5). If you'd rather not publish the generated
# dataset, uncomment the two lines below.
# data/train.jsonl
# data/eval.jsonl
